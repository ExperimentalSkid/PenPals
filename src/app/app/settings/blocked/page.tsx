/* eslint-disable @typescript-eslint/ban-ts-comment, @typescript-eslint/no-explicit-any */
// @ts-nocheck
import Link from "next/link"; import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { unblockUser } from "@/app/app/profile/actions";
export default async function Blocked(){const db=await createClient();const{data}=await db.auth.getClaims();if(!data?.claims?.sub)redirect('/sign-in');const {data:rows}=await db.from('profile_blocks').select('blocked_id,profiles(username,display_name)').eq('blocker_id',data.claims.sub);return <main className="min-h-screen w-full bg-[#f7f5ef] px-6 py-10 text-[#16251f] lg:px-16 lg:py-12"><div className="mx-auto w-full max-w-3xl"><Link href="/app/settings" className="text-sm font-medium text-[#087456] hover:underline">← Back to settings</Link><p className="mt-12 text-xs font-bold uppercase tracking-[.2em] text-[#087456]">Privacy</p><h1 className="mt-3 font-serif text-5xl tracking-[-0.02em] text-[#10231d]">Blocked users</h1><p className="mt-3 text-lg text-black/60">Blocked users can&apos;t view your profile or contact you. You can unblock them at any time.</p><div className="mt-10 divide-y divide-black/10 border-t border-black/10">{(rows??[]).map((r:any)=><div key={r.blocked_id} className="flex items-center justify-between gap-4 py-5"><div><p className="font-medium text-[#10231d]">{r.profiles?.display_name || "Blocked profile"}</p>{r.profiles?.username && <p className="mt-1 text-sm text-black/50">@{r.profiles.username}</p>}</div><form action={unblockUser}><input type="hidden" name="blocked_id" value={r.blocked_id}/><button className="rounded-md border border-black/15 px-3 py-2 text-sm text-black/65 hover:bg-white/50">Unblock user</button></form></div>)}{!rows?.length && <p className="py-10 text-sm text-black/50">You have not blocked anyone.</p>}</div></div></main>}
